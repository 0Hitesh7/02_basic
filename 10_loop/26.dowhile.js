let retry = 0;
do {
    console.log(retry);
    retry++;
} while (retry < 3);
console.log();





// dowhile mostly use in automation to find locator
let retry1 = 0;
do {
    console.log("Finding locator, retrying: " + retry1);
    retry1++;
} while (retry1 < 3);